import React from 'react';

const Header = () => {
    const country = 'Japan';

    return (
        <div className={'mb-14'}>
            <h2>2025 starts in {country} in</h2>
        </div>
    );
};

export default Header;